---
title: "Crabs"
format: html
editor: visual
echo: false
---

## 

```{r}
#install.packages("lterdatasampler")
```

```{r, include: FALSE}

library(lterdatasampler)
library(ggplot2)
library(tidyverse)
View(pie_crab)
```

```{r}
#| fig-cap: "this is my caption"
ggplot(data = pie_crab, aes(
  y = latitude, 
  x = size)
) + geom_point()
  
    
```
