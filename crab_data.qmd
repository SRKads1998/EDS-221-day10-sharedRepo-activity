---
title: "Crabs"
format: html
editor: visual
---

## 

```{r}
#install.packages("lterdatasampler")
```

```{r}
library(lterdatasampler)
library(ggplot2)
library(tidyverse)
View(pie_crab)
```

```{r}
ggplot(data = pie_crab, aes(
  y = latitude, 
  x = size)
) + geom_point()
    
```
